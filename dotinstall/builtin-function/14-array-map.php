<?php

// ==========================================================================
// array_map
// ==========================================================================

// 指定した関数で処理された新しい配列を返す

$prices = [100, 200, 300];

$newPrices = array_map(
  // function ($n) {
  //   return $n * 1.1;
  // },
  fn ($n) => $n * 1.1, // アロー関数で書いた場合
  $prices
);

print_r($newPrices);
